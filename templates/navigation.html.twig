<nav class="navbar  navbar-expand-lg bg-light">
			<div class="container">
				<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" 
				aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
					<span class="navbar-toggler-icon"></span>
				</button>
				<div class="collapse navbar-collapse" id="navbarSupportedContent">
				<a class="navbar-brand"href="{{ path('app_home')}}"><img src="{{ asset('images/logo-text.png')}}" width="90" height="30" alt="logo - Eco IT" /></a>
					<ul class="navbar-nav  me-auto mb-2 mb-lg-0 ">
						<li class="nav-item">
							<a class="nav-link active" aria-current="page" href="{{ path('app_home')}}">Accueil</a>
						</li>
            			<li class="nav-item">
							<a class="nav-link " href="{{ path('app_courses')}}">Formations</a>
						</li>
						<li class="nav-item ">
							<a class="nav-link " href="{{ path('app_about')}}">A propos</a>		
						</li>		
					</ul>
					{% if not app.user %}
						<div class="nav-item">	
							<a class="nav-link" href="{{ path('app_login')}}"><button type="button" class="btn btn-outline-secondary">Connexion</button></a>
						</div>
					{% else %}
						<div class="nav-item">	
							<a class="nav-link" href="{{ path('app_logout')}}"><button type="button" class="btn btn-outline-primary">Déconnexion</button></a>
						</div>
					{% endif %}
					{% if is_granted('ROLE_ADMIN') %}
						<div class="nav-item">	
							<a class="nav-link" href="{{ path('app_admin_index')}}"><button type="button" class="btn btn-outline-primary">Admin</button></a>
						</div>
					{% endif %}
				</div>
			</div>
		</nav>


